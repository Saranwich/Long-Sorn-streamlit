# .env อยู่ใน .gitignore ซึ่งไม่สามารถมองเห็นได้ ไฟล์นี้เป็นเพียงตัวอย่างที่ประกออยู่ในไฟล์ .env 
# โดย API Keys ไม่สามารถเปิดให้ใครดูได้ จะลงไว้ใน discord

# --- Local Database Configuration ---
# (ใช้สำหรับ docker-compose เพื่อสร้าง DB container)
DB_USER=longsorn-user
DB_PASSWORD=<DISCORD ROOM: KEY>
DB_NAME=longsorn-db

# --- Supabase Auth Configuration ---
# (ใช้สำหรับ FastAPI application เพื่อคุยกับ Supabase Auth Service)
SUPABASE_URL=<DISCORD ROOM: KEY>
SUPABASE_KEY=<DISCORD ROOM: KEY>
SUPABASE_JWT_SECRET=<DISCORD ROOM: KEY>

# --- Application Settings (from Template) ---
LOG_LEVEL=INFO
DEBUG=true
CORS_ORIGINS=["*"]

# --- Cloudflare R2 Configuration ---
CLOUDFLARE_R2_ENDPOINT_URL=<DISCORD ROOM: KEY>
CLOUDFLARE_R2_ACCESS_KEY_ID=<DISCORD ROOM: KEY>
CLOUDFLARE_R2_SECRET_ACCESS_KEY=<DISCORD ROOM: KEY>
CLOUDFLARE_R2_BUCKET_NAME=long-sorn-storage

# --- Upstash Redis for Job Queue ---
REDIS_URL=<DISCORD ROOM: KEY>
JOB_QUEUE_NAME=long-sorn

# --- Database Creditial ---
DATABASE_URL=<DISCORD ROOM: KEY>